// A Plus B
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);

    for (int n = 0, N = (cin >> N, N); n < N; n++) {
        int l,r; cin >> l >> r;
        cout << l+r << "\n";
    }
    
    return 0;
}
