// Miracle Sort
#include <bits/stdc++.h>
#include <cstdint>
using namespace std;

int main() {
    ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    int N; cin >> N;
    for (int i = 0; i < N; i++) {int64_t ipt; cin >> ipt; cout << ipt << "\n";}

    return 0;
}