{
    "Standard CP Boilerplate": {
        "prefix": "cpoj",
        "description": "Gives you many macros you might need to write a competitive programming solution for standard online judges like DMOJ and Codeforces",
        "body": [
            "// ${1:problem_name}",
            "#include <bits/stdc++.h>",
            "using namespace std;",
            "",
            "typedef long long ll;",
            "typedef pair<int, int> pi;",
            "typedef tuple<int ,int, int> ti;",
            "typedef pair<pi, int> pii;",
            "typedef pair<ll, int> pli;",
            "typedef pair<ll, ll> pl;",
            "typedef tuple<ll,ll,ll> tl;",
            "typedef pair<ll,pl> pll;",
            "typedef pair<string, int> psi;",
            "typedef vector<int> vi;",
            "typedef unordered_set<int> si;",
            "typedef set<ll> sl;",
            "typedef queue<int> qi;",
            "typedef vector<ll> vl;",
            "typedef vector<string> vs;",
            "typedef vector<vi> vvi;",
            "typedef vector<pi> vii;",
            "typedef vector<ti> vti;",
            "typedef vector<pl> vll;",
            "typedef vector<tl> vtl;",
            "typedef vector<bool> vb;",
            "#define endl '\\n'",
            "#define FILL(a, b) memset(a, b, sizeof(a))",
            "#define ALL(x) x.begin(), x.end()",
            "#define LOOP(n) for (int _ = 0; _ < n; _++)",
            "#define pb push_back",
            "",
            "constexpr int MM = 1e5+5;",
            "constexpr int INF = 0x3F3F3F3F;",
            "constexpr ll INFL = 0x3F3F3F3F3F3F3F3FLL;",
            "constexpr int MOD = 1e9 + 7;",
            "",
            "void print(vi arr, bool isRes=false) {",
            "    if (!isRes) {",
            "        printf(\"[\");",
            "        for (int i=0;i<arr.size();i++)",
            "            printf(\"%d%s\", arr[i], (((i+1)<arr.size()) ? \", \" : \"]\\n\"));",
            "    } else {",
            "        for (int i=0;i<arr.size();i++)",
            "            printf(\"%d%c\", arr[i], (((i+1)<arr.size()) ? ' ' : '\\n'));",
            "    }",
            "}",
            "void print(vii arr) {",
            "    printf(\"[\");",
            "    for (int i=0;i<arr.size();i++)",
            "        printf(\"(%d,%d)%s\", arr[i].first, arr[i].second, (((i+1)<arr.size()) ? \", \" : \"]\\n\"));",
            "}",
            "void print(si s) {",
            "    printf(\"{\");",
            "    int n = 1;",
            "    for (int i: s) {",
            "        printf(\"%d%s\", i, (n<s.size()) ? \", \" : \"}\\n\");",
            "        n++;",
            "    }",
            "}",
            "void print(vb arr) {",
            "    printf(\"[\");",
            "    for (int i=0;i<arr.size();i++)",
            "        printf(\"%c%s\", ((arr[i]) ? 'T' : 'F'), (((i+1)<arr.size()) ? \", \" : \"]\\n\"));",
            "}",
            "",
            "int main() {",
            "    ios::sync_with_stdio(false); cin.tie(nullptr); cout.tie(nullptr);",
            "",
            "    $0",
            "",
            "    return 0;",
            "}"
        ]
    },
    "Technical Online Judge CP Boilerplate": {
        "prefix": "tcoj",
        "description": "Gives you many macros you might need to write a competitive programming solution for technical online judges like Leetcode and Hackerrank",
        "body": [
            "// ${1:problem_name}",
            "#include <bits/stdc++.h>",
            "using namespace std;",
            "",
            "typedef long long ll;",
            "typedef pair<int, int> pi;",
            "typedef tuple<int ,int, int> ti;",
            "typedef pair<pi, int> pii;",
            "typedef pair<ll, int> pli;",
            "typedef pair<ll, ll> pl;",
            "typedef tuple<ll,ll,ll> tl;",
            "typedef pair<ll,pl> pll;",
            "typedef vector<int> vi;",
            "typedef unordered_set<int> si;",
            "typedef set<ll> sl;",
            "typedef queue<int> qi;",
            "typedef vector<ll> vl;",
            "typedef vector<string> vs;",
            "typedef vector<vi> vvi;",
            "typedef vector<pi> vii;",
            "typedef vector<ti> vti;",
            "typedef vector<pl> vll;",
            "typedef vector<tl> vtl;",
            "typedef vector<bool> vb;",
            "#define FILL(a, b) memset(a, b, sizeof(a))",
            "#define ALL(x) x.begin(), x.end()",
            "#define LOOP(n) for (int _ = 0; _ < n; _++)",
            "#define pb push_back",
            "",
            "$0",
            ""
        ]
    }
}
